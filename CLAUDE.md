# CLAUDE.md — Financial Truth Engine

This file is a pointer, not a duplicate contract.

**Read `AGENTS.md` in this repo root before doing anything.** It is the
standing operating contract for this repository — hard rules, stack,
canonical commands, Definition of Done, work tiers, and stop-and-ask rules —
and applies equally to Claude Code, Codex, or any other agent working in
`financial-truth-engine/`.

Keeping one contract file (`AGENTS.md`) instead of two avoids the two files
silently drifting apart. If `AGENTS.md` and this file ever disagree,
`AGENTS.md` is authoritative.
